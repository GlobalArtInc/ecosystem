{
  "name": "globalart-passport-example",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "cli": "npm run build && node dist/cli.js",
    "build": "tsc",
    "prestart": "npm run build",
    "precli": "npm run build"
  },
  "dependencies": {
    "@globalart/passport": "workspace:*",
    "@types/node": "^24.9.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
